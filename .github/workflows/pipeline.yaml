name: Quality Analysis

on:
  push:
    paths:
      - 'src/**'
      - 'pom.xml'
      - 'README.md'
      - '.github/workflows/pipeline.yml'
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'src/**'
      - 'pom.xml'
      - '.github/workflows/pipeline.yml'
  workflow_dispatch:

permissions:
  contents: read
  actions: read
  checks: write

jobs:
  build:
    name: Quality Analysis
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Fondamentale per SonarCloud per ricostruire la cronologia dei commit

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: 23
          distribution: 'zulu'
          cache: 'maven'

      - name: Build and Test
        id: maven-build
        run: mvn -B verify

      - name: Sonar Analysis (Forced Update)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # Necessario a Sonar per mappare il progetto e i PR
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}   # Il token segreto di SonarCloud
        run: >
          mvn -B org.sonarsource.scanner.maven:sonar-maven-plugin:sonar
          -Dsonar.projectKey=OOP2026_Gruppo41
          -Dsonar.organization=oop2026
          -Dsonar.host.url=https://sonarcloud.io

      - name: Publish Test Report
        uses: dorny/test-reporter@v1
        if: success() || failure()
        with:
          name: Architectural Tests
          path: target/surefire-reports/*.xml
          reporter: java-junit
          fail-on-error: 'false'
